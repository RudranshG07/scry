[project]
name = "scry-vision"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "opencv-python-headless>=4.10",
    "numpy>=2.0",
    "ultralytics>=8.3",
    "yt-dlp>=2025.1.1",
]

[project.scripts]
scry-vision = "scry_vision.cli:main"

[tool.setuptools]
packages = ["scry_vision"]
